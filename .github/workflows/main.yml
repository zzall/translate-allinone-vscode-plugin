name: CI

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Use Node.js v14.17.1
        uses: actions/setup-node@v2
        with:
          node-version: '14.17.1'
          
      - name: Install
        run: |
          npm install
        
#       - name: publish vscode-plugin-marketplace
#         run: |
#           vsce publish -p 5stsj3ebtg67tbl6tplw6l2yttor2ab7k6bd6kyele54flzbqygq

      - name: echo-test
        run: |
          echo 打印开始
          ls
          echo 打印完毕

      - name: Vscode plugin release action
        # You may pin to the exact commit or the version.
        # uses: JCofman/vscodeaction@611fec1de63d45fef75ab4f28e00021af09f67d1
        uses: JCofman/vscodeaction@v3.0.0
        env:
          PUBLISHER_TOKEN: ${{ secrets.PUBLISHER_TOKEN }}
        with:
          args: publish -p $PUBLISHER_TOKEN

